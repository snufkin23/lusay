# lucisay

AI-powered ASCII art generator built with Go.

## Why?

I wanted to build something like **cowsay**, but smarter. I had cat named Luci, so I used that idea for **lucisay**.
It turns your message into a speech bubble with custom AI-generated ASCII art, making terminal output a fun.
